// import React from 'react'
// import { Card as AntCard } from 'antd';

// const Card = ({ text, data, name, fee}) => {
//   console.log(data);
//   return (
//     <AntCard style={{ width: '100%' }}>
//       <h1>{name ? name : data ? Object.keys(data)[0] : 'Guruh nomi'}</h1>
//       <strong>{fee !== 0 ? fee : '100000'}USD</strong>
//       <strong>{data ? Object.values(data)[0].length : ''} </strong> 
//     </AntCard>
//   )
// }

// export default Card